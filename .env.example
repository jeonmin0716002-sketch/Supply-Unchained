# Supply-Unchained 환경변수 템플릿 (실제 .env 는 커밋 금지 — 그라운드 룰)

# 스캔 결과 SQLite 경로 (기본: data/supply_unchained.db)
SU_DB_PATH=data/supply_unchained.db

# CLI 가 바라볼 API 주소 (기본: http://localhost:8000)
SU_API_URL=http://localhost:8000
